<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Favorite Animals</title>
  <style>
    .extinct {
      color: red;
    }
    .image {
      max-width: 300px;
      max-height: 300px;
    }
  </style>
</head>
<body>
  <h1>List of Favorite Animals</h1>
  <ul>

    <% animals.forEach(animal => { %>
      <li>
        <span class="<%= animal.extinct ? 'extinct' : '' %>">
          <a href="/animals/<%= animal.id %>" ><%= animal.species_name %></a></span> - <i><%= animal.scientific_name %></i>
        <br>
        <img src="<%= animal.image_url %>" alt="<%= animal.species_name %>" class="image">
        <br>
        <%= animal.description %>
      </li>
    <% }) %>
  </ul>
  <a href="/">Home</a>
</body>
</html>
